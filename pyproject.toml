[project]
name = "verse-xml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Linh An", email = "linhan1347@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "lxml>=6.1.2",
]

[project.scripts]
verse-xml = "verse_xml:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
